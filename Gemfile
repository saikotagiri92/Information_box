source 'https://rubygems.org'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails'

# Use mysql2 as the database for Active Record
gem 'mysql2', '0.3.20'

# Use Awesome font-css
gem 'font-awesome-rails'

# Use SCSS for stylesheets
gem 'sass-rails', '~> 4.0.0'

# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'

# Use CoffeeScript for .js.coffee assets and views
gem 'coffee-rails', '~> 4.0.0'

# Use jquery as the JavaScript library
gem 'jquery-rails', "~> 3.0.4"

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 1.2'

# Use devise for authentication
gem 'devise'

group :doc do
  # bundle exec rake doc:rails generates the API under doc/api.
  gem 'sdoc', require: false
end

#Use papercrop for image cropping
gem 'papercrop'

gem 'letter_opener_web'

gem 'pry'
# Use ActiveModel has_secure_password
gem 'bcrypt-ruby'

#Use PaperTrail to keep records of files
gem 'paper_trail', '>= 3.0.0.beta1'

#Use paperclip for attachments
gem "paperclip", :git => "git://github.com/thoughtbot/paperclip.git"

# Use unicorn as the app server
# gem 'unicorn'

#Use jquery-ui-rails for jQuery ui effects in rails
gem 'jquery-ui-rails'

#Use tinymce-rails for tinyMCE wyswyg editor
gem "tinymce-rails"

#Use ancestry for implementing virtual tree structure in models
gem 'ancestry'

#Use will_paginate for pagination
gem 'will_paginate', '~> 3.0'

#Use time_difference for calculating difference in two timestamps
gem 'time_difference'

gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw]

#Use searchkick for elasticsearch
gem "searchkick"

#Use New relic 
gem 'newrelic_rpm'


group :test do
	gem 'rspec'
	gem 'rspec-rails'
	gem 'cucumber-rails', :require => false
end

#Use delayed_job for async jobs
gem "delayed_job", "~> 4.0.0"
gem 'delayed_job_active_record'

group :production do
	 # gem 'pg'
	# gem 'therubyracer'
end